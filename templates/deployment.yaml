apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "helm-nginx.fullname" . }}
  labels:
    app: {{ include "helm-nginx.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "helm-nginx.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "helm-nginx.name" . }}
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
